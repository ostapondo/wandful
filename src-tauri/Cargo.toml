[package]
name = "wandful"
version = "0.0.2"
description = "Wandful — a pixel-art magic wand that casts keyboard shortcuts from mouse gestures"
authors = ["ostapondo"]
edition = "2021"
license = "MIT"
repository = "https://github.com/ostapondo/wandful"
homepage = "https://wandful.app"

[lib]
name = "wandful_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "macos-private-api", "image-png"] }
tauri-plugin-global-shortcut = "2"
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# vendored + patched: see AGENTS.md ("The vendored rdev") for the list — macOS
# drag delivery and TSM-free key events, Windows keyboard-only hooks and a
# message pump that does not end after one message
rdev = { path = "vendor/rdev", features = ["unstable_grab"] }
enigo = "0.6"
uuid = { version = "1", features = ["v4"] }
log = "0.4"
env_logger = "0.11"
base64 = "0.23"

[target.'cfg(target_os = "macos")'.dependencies]
core-foundation = "0.10"
objc2-app-kit = { version = "0.3", features = ["NSRunningApplication", "NSWorkspace", "NSImage", "NSImageRep", "NSBitmapImageRep"] }
objc2 = "0.6"
objc2-foundation = { version = "0.3", features = ["NSData", "NSString", "NSDictionary", "NSArray", "NSGeometry"] }

[target.'cfg(target_os = "windows")'.dependencies]
windows = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_Security",
    # not dead weight: SHGetFileInfoW is gated behind it (it takes a
    # FILE_FLAGS_AND_ATTRIBUTES), and the icons come from that call
    "Win32_Storage_FileSystem",
    "Win32_System_Com",
    "Win32_System_Power",
    "Win32_System_Shutdown",
    "Win32_System_Threading",
    "Win32_UI_Controls",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
] }
png = "0.18"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
